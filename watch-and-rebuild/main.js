import helloText from './sample';

const add = (x, y) => x + y;
const substraction = (x, y) => x - y;
const multi = (x, y) => x * y;
const divide = (x, y) => x / y;

export {
    add,
    substraction,
    multi,
    divide,
    helloText
};
